# Stage 04: Student Model

Purpose:

- define the deployable student architecture
- keep inputs aligned with deploy-time observations
- produce an initialized checkpoint and a config file

Primary script:

- `build_student_model.py`
